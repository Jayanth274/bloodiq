{
  "name": "bloodiq-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd backend && npm install",
    "install": "cd backend && npm install"
  }
}
